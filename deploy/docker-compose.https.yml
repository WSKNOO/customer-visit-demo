services:
  nginx:
    ports:
      - "${GATEWAY_HTTP_PORT:-80}:8080"
      - "${GATEWAY_HTTPS_PORT:-443}:8443"
    volumes:
      - ./nginx-https.conf.example:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_CERT_DIR:-./certs}:/etc/nginx/certs:ro
